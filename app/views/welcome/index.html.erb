<h1> Hi <%=current_user.first_name%> Welcome to Treat Yo Self</h1>
<div><%= link_to "Treat Yo Self?", random_path%>
<%if current_user.lists.any?%>


<%end%>

<div>
  <%if !current_user.lists.any?%>
    <%= link_to "Plan your Treats for the week", new_user_list_path(current_user)%>
  <%else%>
     <%= link_to "Plan your Treats for the week", edit_list_path(current_user)%>
  <%end%>
</div>
